#!/usr/bin/env bash
if [[ -z "${rhasspylisa_ledmanager}" ]]; then
    this_dir="$( cd "$( dirname "$0" )" && pwd )"
    rhasspylisa_ledmanager="${this_dir}/../lib/rhasspylisa_ledmanager"
fi

"${rhasspylisa_ledmanager}/rhasspylisa_ledmanager" "$@"
